"""WireDAQ — a wire-ready data-acquisition (DAQ) architecture simulator.

Subpackages:

* ``wiredaq.protocol``       — the wire format: schema, golden vectors, production codec.
* ``wiredaq.daq_sim``        — the simulator: ports, transports, nodes, collector, sinks.
* ``wiredaq.ground_station`` — receivers, loggers, and the live dashboard sink.
* ``wiredaq.cli``            — the runnable slices (``wiredaq-slice`` / ``wiredaq-serial``).
"""

__version__ = "0.1.0"
